#ifndef PRUEBAS_H
#define PRUEBAS_H

/* 
 * Se prueban los constructores y si devuelven los valores apropiados con los getters (primero el por defecto, luego el parametrizado y luego el de copia)
 * Luego se prueban los setters comprobando con los getters
 * se prueba las funciones ingresar() de manera automatizada y mostrar() de manera supervisada
 * se prueban los getters y setters restantes
 */
void pruebaCuentaBancaria();

/*
 * Diseño de las pruebas:
 * caso1: constructor por defecto
 * caso2: set
 */
void pruebaUsuario();

void pruebaPatinete();

#endif // PRUEBAS_H
